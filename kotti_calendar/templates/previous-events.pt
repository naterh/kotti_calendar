<div tal:condition="events"
     id="upcoming-events"
     i18n:domain="kotti_calendar">
  <h4 i18n:translate="">Previous Releases</h4>
  <dl>
    <tal:repeat tal:repeat="event events">
      <dt class="upcoming-event">
          <a href="${api.url(event)}" title="${event.description}">
            ${event.title}
          </a>
      </dt>
      <dd>
          <h6><span tal:define="format event.all_day and api.format_date or api.format_datetime">
            ${format(event.start)}
            <span tal:condition="event.end">-
            ${format(event.end)}
            </span>
          </span></h6>
          <p>${event.description}</p>
      </dd>
    </tal:repeat>
  </dl>
</div>
